<div class="row">
  <div class="col-md-6 col-md-offset-3 content mas">
    <h1>Vote for the songs!</h1>
    <div class="pas">
      <%= link_to "Search Again!", songs_search_path %>

      <% @songs.each do |song| %>
      <div>
        <%= link_to fa_icon("arrow-circle-o-up"), vote_path(id: song.id ), params: { vote: '1'} %>
        <%= link_to fa_icon("arrow-circle-o-down"), vote_path(id: song.id ), params: { vote: '-1'}%>
        <%= song.title %>
        <%= song.artist %>
      </div>
      <% end %>
    </div>
  </div>
</div>
